# Lead Qualification Run — 2026-06-22

**Status: DEGRADED — ran in manual fallback mode, not via the repo's automation.**

## Why this run could not execute as specified

The task asks to search Instagram, Google Maps, Facebook, and local directories,
and to collect per-lead `follower count`, `review count`, and `Google rating`,
plus assess website quality. In this environment that could not run as designed:

1. **No Instagram credentials.** `main.py` scrapes IG via `instagrapi`, which
   requires `IG_USERNAME` / `IG_PASSWORD`. Both are unset in this environment,
   so the core scraper cannot run at all.
2. **No Google Maps / Google Business access.** No API key or scraping path is
   configured, so Google rating and review counts cannot be collected.
3. **No target geographic area specified.** The task says "within specified
   geographic areas" but no city/state/region is configured anywhere.
4. **`industries.py` is a stub** (`INDUSTRIES = {}`), so even messaging
   personalization is non-functional.
5. **Fallback web tools hit auth walls.** Unauthenticated fetches of Facebook
   and Yelp business pages return **HTTP 403**, so follower counts, review
   counts, and Google ratings cannot be extracted. Those fields were left
   blank rather than fabricated.

## What was actually done

A manual web-research pass cross-referencing business names against search
results to verify the single most reliable signal: **whether a business has a
dedicated website** (the strongest "needs a website" qualifier). Phone/email
were captured only where they appeared verbatim in search snippets.

## Qualified high-priority leads (verified: no dedicated website)

| Business | Category | Location | Phone | Email | Website |
|---|---|---|---|---|---|
| Garritts Power Washing and Junk Removal LLC | pressure washing / junk removal | Old Saybrook, CT | (860) 853-0045 | — | **None** (FB + Yelp only) |
| Jerry's Gutter Cleaning and Power Washing LLC | pressure washing / gutter cleaning | New Fairfield, CT | (914) 299-9844 | info@jerryscleaning.com | **None** (FB + directories only) |

Disqualified (already have functional websites): Anytime Junk Removal (Grand
Haven, MI), A1 Services (Albuquerque, NM), Rockland Junk Removal (Congers, NY).

> Review counts, Google ratings, and follower counts are **not** included for
> any lead because they could not be collected without fabrication. Verify
> these manually before outreach.

## To make this routine work at scale, configure:

- `IG_USERNAME` / `IG_PASSWORD` (env / Railway dashboard) for the IG scraper.
- A Google Places/Maps API key (for ratings + review counts).
- A target geographic area (cities/states/zips).
- Real data in `industries.py`.
